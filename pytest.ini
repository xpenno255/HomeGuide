[pytest]
testpaths = tests
# So `pytest` finds the app package, not just `python -m pytest`
pythonpath = .
addopts = -q --strict-markers
markers =
    corpus: needs a real manual and the real embedding model (set HOMEGUIDE_TEST_PDF)
